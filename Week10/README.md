学习笔记

正常流

flex

grid

```javascript
分行
  根据主轴尺寸，把元素分进行
  若设置了no-wrap，则强行分配进第一行
计算主轴方向
  找出所有flex元素
  把主轴方向的剩余尺寸按比例分配给这些元素
  若剩余空间为负数,所有flex元素为0,等比例压缩剩   余元素
计算交叉轴
  根据每一行最大元素尺寸计算行高
  根据行高flex-align和item-align,确定元素具体位置
第一步总结
绘制需要依赖一个图形环境
我们这里采用了npm包images
绘制在一个viewport上进行上进行
绘制相关的属性：background-color、border、
background-Image

第二部总结
递归调用子元素的绘制方法完成DOM树的绘制
忽略一些不需要绘制的节点
实际浏览器中，文字绘制是难点，需要依赖字体库，我们这里忽略
实际浏览器中，还会对一些图层做compositing,我们这里也忽略了
```

```javascript
总结
  课程的目的是了解浏览器工作的原理，步骤是首先tcp发送文本信息，信息包含请求头，状态和body等。通过状态机解析文本信息解析出body内容，获得html文本，通过状态机解析html文本获得dom树，再计算出每个元素的css，转化为flex流布局额位置信息，最后渲染到位图。
```

